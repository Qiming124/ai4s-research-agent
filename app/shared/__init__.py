# =============================================================================
# shared 包：服务端与客户端共用的数据结构。
#
# 接口两端都会被导入：
#     - server/api/chat.py  → 用于请求/响应校验
#     - client/cli.py        → 用于构造 HTTP 请求体
#
# 所有数据模型定义在 schemas.py 中，本包只做命名空间聚合。
# =============================================================================
